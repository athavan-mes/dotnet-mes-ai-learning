
name: Daily Study Reminder

on:
  schedule:
    - cron: '30 3 * * *'   # Runs daily at 09:00 IST
  workflow_dispatch:

jobs:
  reminder:
    runs-on: ubuntu-latest
    steps:
      - name: Daily Reminder
        run: |
          echo "📘 Today's task: Check your PDF + GitHub issue and make progress!"
